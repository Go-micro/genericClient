syntax = "proto3";

package GenericClient;

service Generic {
    rpc Printify (Request) returns (Response) {
    }
}

message Request {
    bytes body = 1;
}

message Response {
    bytes body = 1;
}